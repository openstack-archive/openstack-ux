============
Installation
============

At the command line::

    $ pip install openstack-ux

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv openstack-ux
    $ pip install openstack-ux
